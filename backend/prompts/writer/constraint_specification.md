## Quantified Constraint Specification
1. 禁止使用无量化边界词作为结论依据：`小型/大型/通常/许多/极慢/可能/显著`。
2. 涉及规模、频率、延迟、寿命、可靠性时，必须给出可观察阈值或区间。
3. 阈值无法确定时，使用 `assumption:` 前缀并在 `missing_evidence_items` 记录证据缺口。
4. 反例与边界条件必须包含触发条件（例如负载区间、部署规模、周期窗口）。
5. 不允许“经验判断”替代量化约束；如无证据必须降级为待验证假设。
