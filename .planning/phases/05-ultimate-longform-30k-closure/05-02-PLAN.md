---
phase: 05-ultimate-longform-30k-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/app/schemas/task.py
  - backend/app/services/task_decomposer.py
  - backend/app/services/pipeline_orchestrator.py
  - backend/app/services/dag_scheduler.py
  - backend/app/services/stage_contracts.py
  - backend/tests/test_task_decomposer.py
  - backend/tests/test_dag_scheduler.py
autonomous: true
---

# Phase 5 Plan 02: 30k-Aware Planning And Adaptive Word Budget

<objective>
将长度控制从“终态兜底补写”前移为“规划即预算”，保证 30k 任务从拆章开始就可达成。
</objective>

## Open-Source Reference
- `arxiv.org/abs/2408.07055` (LongWriter / AgentWrite decomposition)
- `openreview.net/pdf?id=3A71qNKWAS` (LongGenBench long-output adherence)

## Tasks

<task type="code">
  <name>Dynamic chapter planning by target_words</name>
  <files>backend/app/services/task_decomposer.py, backend/app/schemas/task.py, backend/app/services/pipeline_orchestrator.py</files>
  <action>新增 30k+ 规划策略：章节数量和每章预算按 target_words 自适应，而不是仅按 depth 固定区间；支持 ultra-longform profile。</action>
  <verify>decomposer 测试覆盖 30k/50k 输入，章节数与预算分布符合策略阈值。</verify>
  <done>30k 任务默认生成可执行且预算合理的 DAG。</done>
</task>

<task type="code">
  <name>Per-node budget ledger and gate</name>
  <files>backend/app/services/dag_scheduler.py, backend/app/services/stage_contracts.py</files>
  <action>引入 planned_words/floor/ceiling 预算账本；writer/assembly 节点按账本验收，支持 gap-aware 调整而非固定阈值。</action>
  <verify>scheduler 测试验证预算分配、短稿拒收、重试/补写决策正确。</verify>
  <done>长度控制从终态单点 gate 升级为全链路预算控制。</done>
</task>

<task type="code">
  <name>Adaptive expansion policy</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>把 auto-expansion 从固定波次升级为“剩余缺口 + 风险 + 成本”自适应策略，并输出决策理由。</action>
  <verify>模拟大缺口任务时可稳定插入补写节点直至达标或触发明确成本上限失败。</verify>
  <done>30k 任务长度失败率显著下降，失败原因可解释。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_task_decomposer.py tests/test_dag_scheduler.py`
- `cd backend && .venv/bin/pytest -q tests/test_e2e_flows.py -k "target_words or expansion or long"`

## Success Criteria
- [ ] 30k+ 任务 DAG 拆章与预算分布可预测且可测试。
- [ ] 节点预算账本驱动调度决策，不依赖单次终态补救。
- [ ] 长度不足时能给出结构化扩写策略与失败透明度。
