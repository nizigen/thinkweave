---
phase: 05-ultimate-longform-30k-closure
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - backend/app/services/long_text_fsm.py
  - backend/app/services/dag_scheduler.py
  - backend/app/prompts/consistency/check.md
  - backend/app/prompts/writer/write_chapter.md
  - backend/app/services/stage_contracts.py
  - backend/tests/test_long_text_fsm.py
  - backend/tests/test_dag_scheduler.py
autonomous: true
---

# Phase 5 Plan 03: Assembly Editor + Robust Consistency Repair

<objective>
将终稿阶段从“章节拼接”升级为“全稿编辑收敛”，并把 consistency 修复链路做成可收敛、可审计的策略。
</objective>

## Open-Source Reference
- `storm-project.stanford.edu/research/storm/` (strong pre-writing + staged refinement)
- `langchain-ai/open_deep_research` quality test patterns

## Tasks

<task type="code">
  <name>Dedicated assembly rewrite stage</name>
  <files>backend/app/services/long_text_fsm.py, backend/app/services/dag_scheduler.py, backend/app/services/stage_contracts.py</files>
  <action>新增 assembly editor 节点（或等价角色）做全稿过渡、术语统一、重复折叠、结论收敛，替换纯 join 终稿路径。</action>
  <verify>finalize 前存在 assembly 收敛节点；输出结构和连贯性指标优于 join-only 基线。</verify>
  <done>终稿质量不再依赖章节拼接偶然性。</done>
</task>

<task type="code">
  <name>Hybrid consistency checking</name>
  <files>backend/app/prompts/consistency/check.md, backend/app/services/dag_scheduler.py</files>
  <action>一致性检查采用“章节摘要优先 + 全文关键片段回查”混合模式；输出必须包含 severity 和 repair_priority。</action>
  <verify>consistency 输出契约测试通过；高严重度问题可稳定触发定向修复。</verify>
  <done>一致性结论更稳定，误判/漏判率下降。</done>
</task>

<task type="code">
  <name>Repair budget manager</name>
  <files>backend/app/services/dag_scheduler.py, backend/tests/test_dag_scheduler.py</files>
  <action>将 consistency/review repair 从固定重试升级为预算管理（按问题严重度和剩余风险动态分配）。</action>
  <verify>超过预算时输出结构化失败报告；预算内问题可收敛到 pass=true。</verify>
  <done>Consistency failed 的“无效重试”减少，收敛逻辑可解释。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_long_text_fsm.py tests/test_dag_scheduler.py -k "consistency or repair or finalize"`
- `cd backend && .venv/bin/pytest -q tests/test_agent_prompt_contracts.py`

## Success Criteria
- [ ] 全稿 assembly 收敛节点接入主链并可回归测试。
- [ ] consistency 输出包含 severity/priority 并驱动修复策略。
- [ ] 修复预算机制可收敛或透明失败，避免黑盒反复重试。
