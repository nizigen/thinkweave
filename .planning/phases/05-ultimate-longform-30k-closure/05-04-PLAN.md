---
phase: 05-ultimate-longform-30k-closure
plan: "04"
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - backend/tests/test_longform_eval.py
  - backend/tests/test_e2e_flows.py
  - scripts/playwright_cli_real_env.sh
  - scripts/longform_eval_runner.sh
  - docs/progress.md
  - docs/IMPLEMENTATION_PLAN.md
autonomous: true
---

# Phase 5 Plan 04: 30k Benchmark Gate And Real-Env E2E

<objective>
建立“30k 长文可发布”验收体系：自动评测 + 真实链路 e2e + 明确的 pass/fail 门槛。
</objective>

## Open-Source Reference
- `langchain-ai/open_deep_research/src/legacy/tests/test_report_quality.py`
- `openreview.net/pdf?id=3A71qNKWAS` (long generation adherence metrics)

## Tasks

<task type="code">
  <name>Longform evaluation harness</name>
  <files>backend/tests/test_longform_eval.py, scripts/longform_eval_runner.sh</files>
  <action>新增 30k 评测集与指标：length compliance、instruction adherence、citation coverage、duplicate rate、consistency severity budget。</action>
  <verify>评测脚本输出 machine-readable JSON，支持 CI/夜间任务。</verify>
  <done>30k 能力有持续、可比较的量化指标。</done>
</task>

<task type="code">
  <name>Real-environment 30k e2e flow</name>
  <files>scripts/playwright_cli_real_env.sh, backend/tests/test_e2e_flows.py</files>
  <action>新增 longform 参数模式（report/deep/target_words=30000），脚本输出阶段进度、证据指标、终态质量摘要。</action>
  <verify>真实环境下可重复运行并在超时时给出可解释阻塞原因。</verify>
  <done>从 UI/API 到终稿的 30k 链路具备可审计证据。</done>
</task>

<task type="doc">
  <name>Gate definition and rollout record</name>
  <files>docs/progress.md, docs/IMPLEMENTATION_PLAN.md</files>
  <action>写入 30k gate 阈值、灰度策略、已知风险、回滚策略。</action>
  <verify>文档与测试/脚本一致，避免口径漂移。</verify>
  <done>团队对“何时算达标”有统一、可执行标准。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_longform_eval.py tests/test_e2e_flows.py`
- `TARGET_WORDS=30000 DEPTH=deep MODE=report WAIT_FOR_COMPLETION=1 COMPLETION_TIMEOUT_SECS=3600 TUNNEL_PROVIDER=local TASK_TOKEN=test-token-123 bash scripts/playwright_cli_real_env.sh`
- `bash scripts/longform_eval_runner.sh`

## Success Criteria
- [ ] 30k 评测集和指标稳定可复现。
- [ ] 真实链路 30k 任务可得到通过或可解释失败输出。
- [ ] 交付门槛被文档化并纳入日常回归。
