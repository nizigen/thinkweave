---
phase: 04-web-visibility-and-e2e
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/tasks.py
  - backend/app/services/task_service.py
  - backend/app/schemas/task.py
  - backend/app/routers/ws.py
  - backend/app/schemas/ws_event.py
autonomous: true
---

# Phase 4 Plan 01: Observability Contract Alignment

<objective>
对齐任务详情与监控事件契约，让 Web 监控能稳定呈现阶段语义、DAG 状态和阻塞原因。
</objective>

## Tasks

<task type="code">
  <name>任务详情诊断字段对齐</name>
  <files>backend/app/services/task_service.py, backend/app/schemas/task.py, backend/app/routers/tasks.py</files>
  <action>统一任务详情输出字段，确保前端可读取 stage 进度、node 概览、blocking_reason、关键重试/repair 提示。</action>
  <verify>task API 相关测试通过，字段语义稳定。</verify>
  <done>任务详情响应可直接驱动监控页诊断展示。</done>
</task>

<task type="code">
  <name>WebSocket 事件语义对齐</name>
  <files>backend/app/routers/ws.py, backend/app/schemas/ws_event.py</files>
  <action>校准 ws 事件负载与命名，保证阶段化进度与 DAG 节点状态映射一致，避免前端二义性解析。</action>
  <verify>ws 相关测试通过；关键事件字段完整。</verify>
  <done>监控事件可稳定映射到前端状态模型。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_task_api.py`
- `cd backend && .venv/bin/pytest -q tests/test_ws_endpoint.py tests/test_ws_manager.py tests/test_ws_integration.py`
- `cd backend && .venv/bin/pytest -q tests/test_task_service_entry_stage.py`

## Success Criteria
- [ ] 任务详情返回包含可诊断的阶段化字段与阻塞原因。
- [ ] ws 事件契约与前端监控语义一致。
- [ ] 关键后端回归测试稳定通过。
