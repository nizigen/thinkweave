---
phase: 04-web-visibility-and-e2e
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/src/pages/MonitorPage.jsx
  - frontend/src/lib/apiBase.js
  - scripts/playwright_cli_real_env.sh
  - backend/tests/test_e2e_flows.py
autonomous: true
---

# Phase 4 Plan 02: Monitor UI Alignment And E2E Proof

<objective>
让 Monitor 页面与后端可观测契约一致，并以 real-env E2E 脚本形成可重复验证证据。
</objective>

## Tasks

<task type="code">
  <name>监控页字段映射与异常可解释性</name>
  <files>frontend/src/pages/MonitorPage.jsx, frontend/src/lib/apiBase.js</files>
  <action>按 Phase 4 的观测契约对齐页面字段映射，明确展示 stage 进度、DAG 节点状态和 blocking_reason。</action>
  <verify>手工 smoke + 相关前端测试（若存在）通过。</verify>
  <done>监控页在非终态场景可解释，不依赖日志排查。</done>
</task>

<task type="code">
  <name>real-env E2E 回归脚本收口</name>
  <files>scripts/playwright_cli_real_env.sh, backend/tests/test_e2e_flows.py</files>
  <action>补齐脚本断言链路：UI 创建任务 -> 监控可见关键状态 -> 终态或可解释阻塞；并固化到 e2e 测试口径。</action>
  <verify>real-env 脚本在目标窗口内给出稳定 pass 或可解释 issue 输出。</verify>
  <done>Phase 4 交付具备真实链路可复验证据。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_e2e_flows.py`
- `TUNNEL_PROVIDER=local WAIT_FOR_COMPLETION=1 COMPLETION_TIMEOUT_SECS=120 POLL_INTERVAL_SECS=2 TASK_TOKEN=test-token-123 bash scripts/playwright_cli_real_env.sh`

## Success Criteria
- [ ] Monitor 页面与阶段化可观测契约对齐。
- [ ] 真实链路 E2E 可重复执行并输出可审计结果。
- [ ] 失败场景默认输出可解释原因而非黑盒超时。
