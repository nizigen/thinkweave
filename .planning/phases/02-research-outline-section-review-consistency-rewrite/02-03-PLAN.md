---
phase: 02-research-outline-section-review-consistency-rewrite
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - backend/app/routers/tasks.py
  - backend/app/services/task_service.py
  - backend/tests/test_task_api.py
  - scripts/playwright_cli_real_env.sh
autonomous: true
---

# Phase 2 Plan 03: API And E2E Stability

<objective>
验证 research-first 重写后的 API 输出与真实流程执行稳定性。
</objective>

## Tasks

<task type="code">
  <name>任务详情可观测性增强</name>
  <files>backend/app/services/task_service.py, backend/app/schemas/task.py</files>
  <action>确保任务详情返回可诊断字段，便于非终态定位与监控。</action>
  <verify>task api 测试通过。</verify>
  <done>API 层具备排障所需透明度。</done>
</task>

<task type="code">
  <name>真实环境脚本回归</name>
  <files>scripts/playwright_cli_real_env.sh</files>
  <action>覆盖 UI 创建链路与 API 断言，保持失败时可解释输出。</action>
  <verify>real-env 脚本可通过创建断言；短超时场景输出阻塞原因。</verify>
  <done>Phase 2 主链路具备可回归的 E2E 证据。</done>
</task>

## Verification
- `backend/tests/test_task_api.py`（与任务详情/创建相关子集）
- `scripts/playwright_cli_real_env.sh`（`WAIT_FOR_COMPLETION=0` + 短超时）

## Success Criteria
- [ ] API 输出可支持重写后流程诊断。
- [ ] UI 创建到任务落库可稳定回归。
- [ ] 非终态具备明确阻塞理由。
