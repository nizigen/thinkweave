---
phase: 02-research-outline-section-review-consistency-rewrite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/pipeline_orchestrator.py
  - backend/app/services/task_decomposer.py
  - backend/app/services/task_service.py
  - backend/app/schemas/task.py
autonomous: true
---

# Phase 2 Plan 01: Research-First Decomposition Backbone

<objective>
将主生成链重写为 research-first：先研究、再成稿，并确保 DAG/阶段元数据一致。
</objective>

## Tasks

<task type="code">
  <name>Research-first DAG 约束</name>
  <files>backend/app/services/task_decomposer.py, backend/app/services/pipeline_orchestrator.py</files>
  <action>确保至少存在 researcher 节点，writer 节点依赖 researcher，输出符合新角色契约。</action>
  <verify>task decomposer 与 entry stage 测试通过。</verify>
  <done>创建任务后 DAG 体现 research-first 链路。</done>
</task>

<task type="code">
  <name>任务创建路径接入新编排</name>
  <files>backend/app/services/task_service.py, backend/app/schemas/task.py</files>
  <action>创建任务时通过 PipelineOrchestrator 写入阶段化元数据，并保持返回结构兼容。</action>
  <verify>task api/task service 关键路径测试通过。</verify>
  <done>任务入口稳定产出 research-first DAG + pipeline metadata。</done>
</task>

## Verification
- `backend/tests/test_task_decomposer.py`
- `backend/tests/test_task_service_entry_stage.py`

## Success Criteria
- [ ] DAG 中 researcher 节点存在且 writer 依赖 researcher。
- [ ] 任务创建路径返回结构稳定。
- [ ] research-first 编排与阶段化元数据共存。
