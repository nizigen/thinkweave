---
phase: 07-halo-tea-dag-recompose
plan: "04"
type: execute
wave: 1
depends_on: ["07-01", "07-02", "07-03"]
requirements_addressed: [R14]
files_modified:
  - backend/app/services/dag_recomposer.py
  - backend/app/services/dag_scheduler.py
  - backend/tests/test_dag_recomposer.py
  - backend/tests/test_dag_scheduler.py
autonomous: true
---

# Phase 7 Plan 04: Dynamic DAG Recomposition Runtime Path

<objective>
新增 `dag_recomposer` 运行时重组规划模块，并接入一致性失败后的修复波次注入路径，实现结构化“插入 + 重连”决策输出与执行。
</objective>

<threat_model>
- Threat: recomposition plan breaks current repair wave behavior. Mitigation: preserve existing title/role/status conventions and validate with existing scheduler tests.
- Threat: unbounded wave insertion. Mitigation: enforce existing wave cap (`MAX_CONSISTENCY_REPAIR_WAVES`) inside recomposer plan.
- Threat: dependency graph corruption. Mitigation: logical-id two阶段 materialization，先建映射再解引用 `depends_on`。
</threat_model>

## Tasks

<task type="code">
  <name>Add structured DAG recomposition planner</name>
  <files>backend/app/services/dag_recomposer.py</files>
  <action>实现一致性修复波次重组计划：目标归一化、波次限制、插入节点计划、重连依赖描述。</action>
  <verify>`pytest -q backend/tests/test_dag_recomposer.py`</verify>
  <acceptance_criteria>支持 quick_compact 与 review_chain 两种模式；返回 should_inject/reason/insertions/reconnects。</acceptance_criteria>
</task>

<task type="code">
  <name>Integrate recomposer into scheduler injection path</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>将 `_inject_consistency_repair_wave` 改为消费 `build_consistency_recompose_plan()`，按 logical-id 执行节点插入与依赖重连。</action>
  <verify>`pytest -q backend/tests/test_dag_scheduler.py -k "inject_repair_wave"`</verify>
  <acceptance_criteria>既有 quick/standard 注入行为保持通过；writer/reviewer/consistency 依赖链正确。</acceptance_criteria>
</task>

## Verification

- `cd backend; .\\.venv\\Scripts\\python.exe -m pytest -q tests/test_dag_recomposer.py`
- `cd backend; .\\.venv\\Scripts\\python.exe -m pytest -q tests/test_dag_scheduler.py -k "inject_repair_wave"`
- `cd backend; .\\.venv\\Scripts\\python.exe -m pytest -q tests/test_dag_eval.py tests/test_mcts_engine.py tests/test_tea_protocol.py`

## Success Criteria

- [ ] 动态重组具备结构化计划输出（insertions + reconnects）。
- [ ] 调度器运行时可按计划插入并连线新节点。
- [ ] 既有修复波次行为不回归。

