---
phase: 01-stage-skeleton-schema-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/app/services/dag_scheduler.py
  - backend/app/schemas/task.py
  - backend/app/routers/ws.py
  - scripts/playwright_cli_real_env.sh
autonomous: true
---

# Phase 1 Plan 03: Transparency And E2E Closure

<objective>
完成失败透明化与真实环境端到端验收，收口 Phase 1。
</objective>

## Tasks

<task type="code">
  <name>失败透明化补齐</name>
  <files>backend/app/services/dag_scheduler.py, backend/app/schemas/task.py, backend/app/routers/ws.py</files>
  <action>统一暴露失败原因、重试原因、阶段状态于 API/WS/监控。</action>
  <verify>终态失败可解释且链路一致。</verify>
  <done>error_message 和原因链可见。</done>
</task>

<task type="code">
  <name>orphan running 恢复强化</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>服务重启后可回收卡住的 running 节点并恢复调度。</action>
  <verify>重启恢复路径测试通过。</verify>
  <done>无长期 init/running 卡死。</done>
</task>

<task type="test">
  <name>全流程真实环境验收</name>
  <files>scripts/playwright_cli_real_env.sh</files>
  <action>执行创建任务、监控 DAG、完成或失败解释全流程并留存日志。</action>
  <verify>至少 1 条真实题目任务有验收证据。</verify>
  <done>Playwright CLI 关键路径可复现。</done>
</task>

## Verification
- Playwright CLI 关键路径跑通并留存日志。

## Success Criteria
- [ ] 失败可解释（error_message + 原因链）。
- [ ] 监控页持续可见 DAG 推进。
- [ ] 至少 1 条真实任务完成 E2E 验收。
