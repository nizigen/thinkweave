---
phase: 01-stage-skeleton-schema-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/stage_contracts.py
  - backend/app/services/pipeline_orchestrator.py
  - backend/app/services/task_service.py
  - backend/app/services/dag_scheduler.py
autonomous: true
---

# Phase 1 Plan 01: Stage Contract Foundation

<objective>
建立语义阶段契约与入口编排骨架，不破坏现有 DAG 任务分解。
</objective>

## Tasks

<task type="code">
  <name>统一阶段码与契约映射</name>
  <files>backend/app/services/stage_contracts.py</files>
  <action>定义 SCOPING/RESEARCH/OUTLINE/DRAFT/REVIEW/ASSEMBLY/QA 与 alias 映射兼容层。</action>
  <verify>阶段码解析与 alias 向后兼容单测通过。</verify>
  <done>旧阶段码历史数据可读，新阶段码映射稳定。</done>
</task>

<task type="code">
  <name>任务入口阶段化元数据</name>
  <files>backend/app/services/pipeline_orchestrator.py, backend/app/services/task_service.py</files>
  <action>创建任务时在 DAG 分解后注入 pipeline 元数据，包含 execution_graph=dag_preserved。</action>
  <verify>创建任务后可观察 pipeline 元数据字段。</verify>
  <done>入口阶段元数据注入稳定且不影响 DAG 初始化。</done>
</task>

<task type="code">
  <name>节点 payload 注入阶段元数据</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>节点调用注入 stage_code/stage_contract/schema_version。</action>
  <verify>scheduler 关键路径测试通过且无回归。</verify>
  <done>节点级阶段元数据在执行上下文可见。</done>
</task>

## Verification
- 单测覆盖入口注入路径与 scheduler 关键路径。

## Success Criteria
- [ ] 创建任务后可看到 pipeline 阶段元数据。
- [ ] DAG 正常初始化，不因阶段化改动中断。
- [ ] 旧阶段码历史数据可读。
