---
phase: 01-stage-skeleton-schema-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/app/agents/worker.py
  - backend/app/services/dag_scheduler.py
  - backend/prompts/writer/
  - backend/prompts/reviewer/
  - backend/prompts/consistency/
  - backend/prompts/orchestrator/
autonomous: true
---

# Phase 1 Plan 02: Schema Gates And Repair Loop

<objective>
将自由文本容错升级为结构化硬约束与自动修复闭环。
</objective>

## Tasks

<task type="code">
  <name>强化结构化输出 gate</name>
  <files>backend/app/agents/worker.py, backend/app/services/dag_scheduler.py</files>
  <action>writer/reviewer/consistency 严格 schema 校验，失败触发重试并记录原因。</action>
  <verify>schema 非法输出触发重试逻辑并记录错误原因。</verify>
  <done>schema gate 生效且日志可追溯。</done>
</task>

<task type="code">
  <name>consistency 定向修复 DAG 注入</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>当 pass=false 且有 repair_targets 时注入 repair writer/reviewer/consistency 子图。</action>
  <verify>repair_targets 输入可触发修复波次。</verify>
  <done>consistency 失败进入修复路径而非直接终止。</done>
</task>

<task type="code">
  <name>字数补写波次与上限失败</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>终稿字数不足时注入补写波次，超过上限后失败并写入 error_message。</action>
  <verify>字数 gate 与补写路径单测通过。</verify>
  <done>低字数任务不会误判 completed。</done>
</task>

## Verification
- repair 注入与字数 gate 单测通过。

## Success Criteria
- [ ] consistency 失败可进入修复波次。
- [ ] 低字数不会直接 completed。
- [ ] schema 失败可追溯。
