---
phase: 03-repair-dag-final-polish-quality-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/dag_scheduler.py
  - backend/app/services/long_text_fsm.py
  - backend/app/services/stage_contracts.py
  - backend/prompts/consistency/check.md
  - backend/tests/test_dag_scheduler.py
  - backend/tests/test_task_service_entry_stage.py
autonomous: true
---

# Phase 3 Plan 01: Repair Policy Hardening And Final Gate Semantics

<objective>
落地已在 03-CONTEXT.md 锁定的修复与终态策略：
- consistency 失败触发定向 repair wave（上限 2）
- 默认章级定向修复，不回卷 researcher/outline
- final gate 严格执行（长度/结构/证据）
- 超预算后明确 failed，避免静默放行
</objective>

## Tasks

<task type="code">
  <name>一致性失败后的修复触发与轮次上限收敛</name>
  <files>backend/app/services/dag_scheduler.py, backend/app/services/long_text_fsm.py</files>
  <action>统一 consistency 失败后的处理路径：优先注入章级 repair wave，严格受 MAX_CONSISTENCY_REPAIR_WAVES 约束；超限后进入明确终态决策，不做隐式放行。</action>
  <verify>repair wave 相关调度测试通过；失败原因可由 API 读取。</verify>
  <done>修复触发策略与轮次上限具备确定性行为。</done>
</task>

<task type="code">
  <name>最终质量门语义加固</name>
  <files>backend/app/services/dag_scheduler.py, backend/app/services/stage_contracts.py, backend/prompts/consistency/check.md</files>
  <action>确保 final gate 将长度、schema、evidence、冲突检查作为硬约束输入；当 auto expansion 达到上限仍不达标时，任务明确 failed 并输出可解释 reason。</action>
  <verify>finalize 路径测试覆盖 target word ratio、repair budget exhausted、terminal reason。</verify>
  <done>终态语义与失败透明度符合 Phase 3 决策。</done>
</task>

<task type="test">
  <name>策略回归测试补齐</name>
  <files>backend/tests/test_dag_scheduler.py, backend/tests/test_task_service_entry_stage.py</files>
  <action>新增/修正测试，覆盖：repair 注入、轮次上限、final gate 失败语义、blocking_reason 输出稳定性。</action>
  <verify>目标测试集通过；关键失败路径可重复。</verify>
  <done>Phase 3 Wave 1 具备可回归证据。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_dag_scheduler.py -k "repair or consistency or finalize or expansion"`
- `cd backend && .venv/bin/pytest -q tests/test_task_service_entry_stage.py`
- `TUNNEL_PROVIDER=local WAIT_FOR_COMPLETION=1 COMPLETION_TIMEOUT_SECS=60 POLL_INTERVAL_SECS=2 TASK_TOKEN=test-token-123 bash scripts/playwright_cli_real_env.sh`

## Success Criteria
- [ ] consistency 失败 -> repair wave 注入路径稳定且有上限。
- [ ] 超过修复预算后终态为明确 failed（无静默通过）。
- [ ] final gate 失败具备可解释 reason / blocking_reason。
- [ ] 测试与真实脚本能复现并验证上述行为。
