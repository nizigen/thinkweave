---
phase: 03-repair-dag-final-polish-quality-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/app/services/dag_scheduler.py
  - backend/tests/test_dag_scheduler.py
autonomous: true
---

# Phase 3 Plan 02: Quick Repair DAG Convergence Optimization

<objective>
在不改变 Phase 3 锁定策略前提下，压缩 quick 模式 repair 子图，减少 repair 路径节点数量与收敛时延。
</objective>

## Tasks

<task type="code">
  <name>quick repair 子图压缩</name>
  <files>backend/app/services/dag_scheduler.py</files>
  <action>在 `quick + low target_words` 场景，将一致性修复波次压缩为最小链路（writer -> consistency），并限制目标章节数量。</action>
  <verify>repair wave 注入单测覆盖 quick/standard 分支。</verify>
  <done>quick repair 分支节点数显著低于 standard repair 分支。</done>
</task>

<task type="test">
  <name>repair 子图分支测试</name>
  <files>backend/tests/test_dag_scheduler.py</files>
  <action>新增测试验证 quick 分支无 reviewer 节点，standard 分支保留 reviewer 链路。</action>
  <verify>dag_scheduler repair/consistency 相关测试通过。</verify>
  <done>分支行为具备可回归保障。</done>
</task>

## Verification
- `cd backend && .venv/bin/pytest -q tests/test_dag_scheduler.py -k "consistency or repair"`
- `cd backend && .venv/bin/pytest -q tests/test_task_service_entry_stage.py`
- `TUNNEL_PROVIDER=local WAIT_FOR_COMPLETION=1 COMPLETION_TIMEOUT_SECS=60 POLL_INTERVAL_SECS=2 TASK_TOKEN=test-token-123 bash scripts/playwright_cli_real_env.sh`

## Success Criteria
- [ ] quick repair 子图为最小链路。
- [ ] standard repair 子图行为保持不变。
- [ ] 新增测试稳定通过。
- [ ] real-env 运行具备可解释阻塞输出。
