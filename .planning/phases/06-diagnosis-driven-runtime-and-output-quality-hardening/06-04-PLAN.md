---
phase: 06-diagnosis-driven-runtime-and-output-quality-hardening
plan: "04"
type: execute
wave: 2
depends_on: ["06-03"]
requirements_addressed: [R8, R9]
files_modified:
  - backend/app/services/long_text_fsm.py
  - backend/app/agents/fact_check_agent.py
  - backend/app/agents/consistency_agent.py
  - backend/app/services/task_decomposer.py
  - backend/app/schemas/task.py
  - backend/prompts/writer/constraint_specification.md
  - backend/prompts/writer/actionable_output.md
  - backend/prompts/writer/write_chapter.md
  - backend/prompts/consistency/check.md
  - backend/tests/test_long_text_fsm.py
  - backend/tests/test_specialized_agents.py
  - backend/tests/test_task_decomposer.py
  - backend/tests/test_prompt_contracts.py
autonomous: true
---

# Phase 6 Plan 04: Fact Check, Quantified Specificity, Actionability, And Feedback Loop

<objective>
修复技术陈述无验证、反例边界模糊、实施路径不可操作、建议未被执行的问题，形成从写作到检查再到定向返工的闭环。
</objective>

<threat_model>
- Threat: FactCheckAgent falsely validates unsupported technical claims. Mitigation: only PASS claims with registry/source match or explicit evidence binding; unknown claims return NEEDS_SOURCE.
- Threat: domain registry becomes stale. Mitigation: registry entries include version/source metadata and tests use fixtures, not live network.
- Threat: actionability prompts embed external pricing/training claims without evidence. Mitigation: require evidence IDs or mark estimates as assumptions.
- Threat: feedback loop causes infinite rewrites. Mitigation: semantic retry limits from Plan 01 and targeted affected-node routing.
</threat_model>

## Tasks

<task type="code">
  <name>Add FACT_CHECK FSM state and routing</name>
  <files>backend/app/services/long_text_fsm.py, backend/app/schemas/task.py, backend/tests/test_long_text_fsm.py</files>
  <action>在 REVIEWING 后、CONSISTENCY 前新增 FACT_CHECK 状态。Fact-check PASS 进入 CONSISTENCY；FAIL/NEEDS_SOURCE 生成 targeted revision tasks 并进入 RE_REVIEW 或 WRITING 修复路径。状态事件包含 failed_claims、affected_nodes、retry_scope。</action>
  <verify>`pytest -q backend/tests/test_long_text_fsm.py -k "fact_check or re_review"`。</verify>
  <acceptance_criteria>技术 claim 未验证时不会直接进入全局一致性检查或 DONE。</acceptance_criteria>
</task>

<task type="code">
  <name>Create FactCheckAgent with registry-backed validation</name>
  <files>backend/app/agents/fact_check_agent.py, backend/app/agents/__init__.py, backend/tests/test_specialized_agents.py</files>
  <action>新增 FactCheckAgent，提取技术术语和 claim，先查本地 spec registry/source metadata，再检查 evidence binding。返回 PASS/FAIL/NEEDS_SOURCE 和 structured failures。初始 fixture 支持 OPC UA 术语验证，但接口保持通用。</action>
  <verify>`pytest -q backend/tests/test_specialized_agents.py -k "fact_check"`。</verify>
  <acceptance_criteria>OPC UA fixture 中缺少规范来源的技术术语会被标记并给出修复建议。</acceptance_criteria>
</task>

<task type="prompt">
  <name>Add quantified constraint specification prompt</name>
  <files>backend/prompts/writer/constraint_specification.md, backend/prompts/writer/write_chapter.md, backend/tests/test_prompt_contracts.py</files>
  <action>新增并接入 constraint specification prompt：禁止“小型、通常、许多、极慢、可能”等无量化边界词；要求规模、频率、延迟、生命周期等条件用可观察阈值表达，无法确定时标记 assumption/evidence gap。</action>
  <verify>`pytest -q backend/tests/test_prompt_contracts.py -k "constraint or specificity"`。</verify>
  <acceptance_criteria>Writer 合约要求反例和边界条件具备量化阈值或明确 assumption 标记。</acceptance_criteria>
</task>

<task type="code">
  <name>Decompose implementation sections into actionable node types</name>
  <files>backend/app/services/task_decomposer.py, backend/app/schemas/task.py, backend/prompts/writer/actionable_output.md, backend/prompts/writer/write_chapter.md, backend/tests/test_task_decomposer.py</files>
  <action>当任务或章节包含“实施/implementation/how-to/步骤/路径/建议”时，decomposer 生成 ACTIONABLE_CHECKLIST、DECISION_MATRIX、TIMELINE_ESTIMATE、RISK_ASSESSMENT 子节点或 stage metadata。Writer prompt 对这些节点输出固定结构。</action>
  <verify>`pytest -q backend/tests/test_task_decomposer.py backend/tests/test_prompt_contracts.py -k "actionable or implementation"`。</verify>
  <acceptance_criteria>实施类章节不再是单一泛化写作节点，必须产出清单、矩阵、估算、风险。</acceptance_criteria>
</task>

<task type="code">
  <name>Make consistency feedback self-applying</name>
  <files>backend/app/agents/consistency_agent.py, backend/app/services/long_text_fsm.py, backend/prompts/consistency/check.md, backend/tests/test_specialized_agents.py, backend/tests/test_long_text_fsm.py</files>
  <action>ConsistencyAgent 增加 unapplied_recommendations 检查：如果文章后段提出编辑/实施建议，前文相关章节必须已应用或被标记为 future/out-of-scope。未应用建议路由到 affected chapters 定向返工。</action>
  <verify>`pytest -q backend/tests/test_specialized_agents.py backend/tests/test_long_text_fsm.py -k "consistency or recommendation or feedback"`。</verify>
  <acceptance_criteria>文章不能以“应该补充 X”结尾但正文没有执行 X，除非明确标注为后续工作。</acceptance_criteria>
</task>

## Verification

- `cd backend; pytest -q tests/test_long_text_fsm.py tests/test_specialized_agents.py tests/test_task_decomposer.py tests/test_prompt_contracts.py`
- Run mocked OPC UA regression: technical terms without source fail FACT_CHECK; vague counterexample fails specificity; implementation chapter must include checklist/matrix/timeline/risk.
- Run `gitnexus_impact` before editing each existing symbol and `gitnexus_detect_changes()` before commit per AGENTS.md.

## Success Criteria

- [ ] FACT_CHECK state exists and routes failed claims to targeted rewrite.
- [ ] FactCheckAgent validates technical terms against registry/source/evidence metadata.
- [ ] Writer constraints require quantified boundaries for scenarios and counterexamples.
- [ ] Implementation sections are decomposed into actionable structures.
- [ ] Consistency loop detects and routes unapplied recommendations.
